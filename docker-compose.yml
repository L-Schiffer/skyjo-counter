services:
  skyjo:
    build: .
    container_name: skyjo
    restart: unless-stopped
    ports:
      - "3200:3000"
    volumes:
      - skyjo-data:/app/data
    env_file: .env

volumes:
  skyjo-data:
